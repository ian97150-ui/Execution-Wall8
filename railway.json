{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:railway && echo '=== Frontend dist contents ===' && ls -la backend/frontend-dist && cd backend && npm install && npx prisma generate && npm run build && echo '=== Final backend structure ===' && ls -la"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}